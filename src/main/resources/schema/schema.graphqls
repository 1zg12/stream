type Query{
    staffs: [Staff]
    staff(name: String): Staff
    address: [Address]
}

type Staff {
    name: String
    age: Int
    country: String
    salary: Float
    address: Address
}

type Address {
    id: String
    district: String
    postcode: Int
}
