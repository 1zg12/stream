package com.example.stream.controller.dao;

import org.apache.tomcat.jni.Address;
import org.springframework.stereotype.Repository;

@Repository
public class AddressDAO {

    public Address getAddress(String staffId){//N (the number of the staff in the company (1+N)
        return null;
    }
}
